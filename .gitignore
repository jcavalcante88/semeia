# dependências
/node_modules

# build do Next
/.next
/out
next-env.d.ts
*.tsbuildinfo

# segredos — nunca versionar
.env
.env.local
.env*.local

# sistema
.DS_Store
Thumbs.db

# pacote de distribuição do projeto
*.zip

# Reels gerados por scripts/reels.mjs — saida, nao codigo.
# Sao ~100 MP4; o repositorio nao e lugar de arquivo de video.
/reels
